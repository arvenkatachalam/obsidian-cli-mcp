{
  "name": "obsidian-cli-mcp",
  "version": "0.1.0",
  "description": "MCP server for Obsidian CLI and headless sync",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "obsidian-cli-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:security": "vitest run tests/security.test.ts",
    "dev": "tsc --watch"
  },
  "keywords": ["obsidian", "mcp", "cli"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
